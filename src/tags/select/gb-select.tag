<gb-select>
  <gb-raw-select label={ opts.label } clear={ opts.clear } options={ opts.options } hover={ opts.hover } default={ opts.default } update={ opts.update } native={ opts.native }>
    <a>{ label }</a>
  </gb-raw-select>
</gb-select>
