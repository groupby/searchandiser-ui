platform: linux

inputs:
  - name: components

outputs:
  # - name: node_modules
  # - name: typings

run:
  dir: components
  path: sh
  args:
    - -exc
    - |
      yarn
# cp -R ./node_modules ../node_modules
# cp -R ./typings ../typings
