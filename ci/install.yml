platform: linux

inputs:
  - name: components

outputs:
  - name: node_modules

run:
  dir: components
  path: sh
  args:
    - -exc
    - |
      yarn --modules-folder ../node_modules
      cp -R ./node_modules ../node_modules
