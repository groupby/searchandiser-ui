platform: linux

inputs:
  - name: components
  - name: node_modules

run:
  dir: components
  path: sh
  args:
    - -exc
    - |
      git clone ../node_modules ./node_modules
      yarn run test+coverage
