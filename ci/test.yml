platform: linux

inputs:
  - name: components
  - name: node_modules
  - name: typings

run:
  dir: components
  path: sh
  args:
    - -exc
    - |
      cp -R ../node_modules/* ./node_modules
      cp -R ../typings/* ./typings
      yarn run test+coverage
