platform: linux

inputs:
  - name: components
  - name: node_modules
  # - name: typings

run:
  dir: components
  path: sh
  args:
    - -exc
    - |
      mv ../node_modules .
      yarn run test+coverage
# cp -R ../typings/* ./typings
