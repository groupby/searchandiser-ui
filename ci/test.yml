platform: linux

inputs:
  - name: components
  - name: node_modules

run:
  dir: components
  path: sh
  args:
    - -exc
    - |
      cp -R ../node_modules/* ./node_modules
      yarn run test+coverage
